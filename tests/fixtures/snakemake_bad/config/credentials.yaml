# PVX030: a credential-shaped literal. The value must never appear in output.
#
# Deliberately not a vendor-shaped key — see tests/fixtures/nextflow_bad/conf/
# creds.config for why, and tests/conftest.py for where the vendor shapes are
# exercised.
password: "hunter2-not-a-real-password"

# References, not secrets. Neither may be flagged.
token: "${ENV_TOKEN}"
api_key: "{{ vault.google_api_key }}"
