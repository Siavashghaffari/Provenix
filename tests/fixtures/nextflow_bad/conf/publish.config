process {
    withName: 'SUBSAMPLE' {
        // PVX022: 'move' takes results out of the work directory.
        publishDir = [
            path: { "${params.outdir}/sub" },
            mode: 'move'
        ]
    }
}
