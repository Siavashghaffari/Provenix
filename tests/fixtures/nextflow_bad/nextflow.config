params {
    outdir = 'results'
    // PVX004: a branch ref, so the content can change under the same URL.
    ref_fasta = 'https://raw.githubusercontent.com/example/refs/master/genome.fa'
    // PVX004: a moving alias.
    ref_gtf   = 'https://ref.example.org/annotations/latest/genes.gtf'
}
